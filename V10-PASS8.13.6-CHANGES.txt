v10 Pass 8.13.6
DYMO SINGLE-SVG LABEL FIX

What the photo proved:
- The barcode and its text were being paginated onto separate physical labels.

Fix:
- Barcode bars and BLK/BR + artwork name now live inside ONE SVG.
- The browser/DYMO driver cannot split the barcode from its text.
- SVG physical size: 1.80in x 0.58in.
- Page remains exactly 2.00in x 0.75in.
- Code128 still encodes the same permanent SKU.
- No inventory, Square, category, or subcategory logic changed.
