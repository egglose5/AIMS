PASS 10.1

- Removed unsafe startup password reset behavior. Bootstrap admin creation is now explicit opt-in and never resets an existing password.
- Added startup ownership/schema diagnostics for identity users, show-admin mappings, and fulfillment backbone visibility.
- Hardened Vendor Portal authorization to deny by default:
  existing mapped vendor stays vendor-scoped
  mapped show-admin keeps full access
  anonymous is denied
  the current legacy single-owner install is preserved through a narrow one-user bridge until show-admin mapping is completed
- Isolated Scout-owned lead queries from Show Finder-owned lead queries so shared ShowDiscoveryLead rows do not cross-consume.
- Safely deprecated the legacy /scout surface and removed its nav entry in favor of Scout Discovery + Scout Research.
- Protected shared fulfillment workflow from legacy Show Order regressions by making legacy status application monotonic.
- Made finished-scan release behavior durable by preventing production refresh from pulling already released shared fulfillment rows back into NEEDS_PRODUCTION.
