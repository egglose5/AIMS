B3 ACCEPTANCE TEST — CONTROLLED AGENT & TOOL GATEWAY

1. Installer must end with:
   B3 PASS — schema verified, Docker healthy, Brain Core responding.

2. Open http://localhost:8080/brain-core
   Confirm:
   - Gateway = ACTIVE
   - Registered tools = 5
   - Pending approvals = 0 before testing
   - Controlled agent & tool gateway section is visible

3. Click "Run B3 gateway self-test" exactly once.

Expected result:
   - 4 execution results appear.
   - 2 harmless governed reads are COMPLETED/allowed.
   - application-submission request is DENIED.
   - money-spend request is DENIED.
   - success message says 2 governed reads allowed and 2 consequential requests denied.
   - denied diagnostic handlers do NOT report SECURITY FAILURE.

4. Refresh the page.
   Recent gateway executions must still show the 4 requests.
   Denied requests metric must be at least 2.

5. Confirm the B2 persistence-test memory still exists.

If all five checks pass, B3 is accepted and Brain Core v1 can be frozen as KNOWN-GOOD.
