v10 Pass 8.13.8

FIRST PRINT ONLY
- For a new/uninitialized item, enter exactly how many labels you need now: e.g. Black 2, Brown 1.
- Print First Barcodes + Initialize.
- Fully initialized cards are done; clicking them no longer opens barcode/count maintenance.
- Partially initialized cards show only the color(s) that still need their first barcode.

SQUARE SCAN FIX
- The previous printed barcode encoded Mustaine-AI's local permanent ID such as 00007.
- Square POS does not know that number, which is why the phone showed 'No matches found'.
- Printed Code128 now encodes the EXISTING SQUARE SKU for that exact Black/Brown item.
- Mustaine-AI stores/backfills the Square SKU and Production accepts the same barcode.
- Existing local permanent SKU records remain intact internally.

DYMO
- Preserves the Pass 8.13.6 single-SVG barcode + BLK/BR text layout.
