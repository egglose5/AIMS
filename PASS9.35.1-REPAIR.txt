PASS 9.35.1 — HYBRID OPS RUNTIME REPAIR

1. Fixes Show Arm runtime crash in ScoutIntegrationService.GetDateOnly.
   Npgsql returns PostgreSQL DATE as System.DateOnly. The old conversion used
   Convert.ToDateTime(DateOnly), which throws InvalidCastException. The helper
   now explicitly supports DateOnly, DateTime, DateTimeOffset, and parseable text.

2. Corrects the 2026 schedule vendor identity from "Abby" to the actual
   ShowVendorProfile name "Abby Yount". The schedule SQL is idempotent, so
   rerunning it adds the three rows that were previously skipped without
   duplicating the 19 rows already imported.

3. Architectural intent is documented inline in the date adapter because this
   boundary matters when the Show database later moves from local PostgreSQL to Ops.

No VPS setting is changed. Show Arm remains local for regression testing.
